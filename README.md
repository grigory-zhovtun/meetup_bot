# PythonMeetup Bot

Телеграм-бот для организации и проведения митапов. Помогает участникам следить за программой, задавать вопросы спикерам, знакомиться друг с другом и поддерживать мероприятие донатами.

## Функционал

### Для гостей:
- 📅 **Программа:** Просмотр расписания докладов.
- 🗣 **Вопросы:** Возможность задать вопрос спикеру во время выступления.
- 🤝 **Нетворкинг:** Заполнение анкеты и поиск собеседников по интересам.
- 💸 **Донаты:** Возможность поддержать организаторов.

### Для спикеров:
- Получение вопросов от аудитории в реальном времени (через пересылку ботом).

### Для организаторов:
- Админка Django для управления событиями, спикерами и расписанием.
- Просмотр статистики по участникам и вопросам.

## Установка и запуск

### Предварительные требования
- Python 3.10+
- PostgreSQL (или SQLite для разработки)

### 1. Клонирование репозитория
```bash
git clone https://github.com/your-repo/meetup_bot.git
cd meetup_bot
```

### 2. Настройка окружения
Создайте файл `.env` в корне проекта и добавьте следующие переменные:
```env
SECRET_KEY=your_django_secret_key
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Применение миграций
```bash
python manage.py migrate
```

### 5. Создание суперпользователя
```bash
python manage.py createsuperuser
```

### 6. Запуск

#### Запуск Django (Админка)
```bash
python manage.py runserver
```
Админка будет доступна по адресу: http://127.0.0.1:8000/admin/

#### Запуск Бота
> **Важно:** Бот запускается как management-команда Django.

```bash
python manage.py runbot
```

## Структура проекта
- `datacenter/` — Основное приложение Django (модели, админка, management commands).
- `meetup/` — Конфигурация проекта Django.
- `tg_bot/` — Логика телеграм-бота (handlers, keyboards, states).
